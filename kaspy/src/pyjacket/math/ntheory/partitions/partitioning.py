


def partitions(n, k=None):
    """generate all the integer partitions of n consisting of up to k (inclusive) elements.
    If k is not provided, all partitions will be generated    
    """
    
    
    
def n_partitions(n, k=None):
    """Compute the number of integer partitions of n, using up to k elements"""